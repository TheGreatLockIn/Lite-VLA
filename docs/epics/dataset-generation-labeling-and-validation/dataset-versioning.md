# Dataset versioning and documentation

**Epic:** Dataset Generation, Labeling, and Validation (105) · **Jira epic:** VLA-6 · **Story:** VLA-47 / 1035 · **Subtasks:** 10105 (naming), 10106 (stats), 10107 (dataset card)

**Human-readable version (browser):** [`dataset-versioning.html`](dataset-versioning.html)

## Executive summary

VLA-47 packages a **processed dataset release** under `data/processed/vMAJOR.MINOR.PATCH/`: validated JSONL splits, optional augmented images, machine-readable `validation_report.json` (from VLA-45), and human-readable `DATASET_CARD.md`. Version strings are validated before any path is constructed so typos cannot write outside the processed tree.

## Mental model

Think of versioning as **labeling a release artifact**, not git-tagging source code.

It exists because training configs point at `data/processed/v0.1.0/train.jsonl` — without a version folder convention, teammates cannot reproduce which rows and QA stats a run used.

The key engineering tension is **generated docs vs hand-written prose**: the dataset card is rendered from live validation reports so it cannot drift from reality.

A beginner mistake is using `0.1.0` without the `v` prefix, or hand-editing `validation_report.json` after a failed validate.

A senior engineer watches for **artifact lineage** — stats must come from VLA-45, not ad-hoc builder counters.

## Backstory: why this exists

Teams often dump `train.jsonl` in a shared folder and email a spreadsheet of counts. The naive approach loses coupling between QA results and the files Epic 106 actually loads.

That breaks when someone fixes JSONL without updating stats, or training points at `latest/` with ambiguous contents.

So this design chooses semver-style directories, validator-generated JSON reports, and a rendered `DATASET_CARD.md` per version.

## Prerequisites

- VLA-45 validation reports: [dataset-validation.md](dataset-validation.md)
- VLA-43 build output: [synthetic-starter-dataset.md](synthetic-starter-dataset.md)

## Vocabulary

| Term | Meaning in this project |
|------|-------------------------|
| **Processed version** | String matching `^v\d+\.\d+\.\d+$` (e.g. `v0.1.0`) |
| **`validation_report.json`** | Serialized `DatasetValidationReport` for train split |
| **`DATASET_CARD.md`** | Human summary generated by `render_dataset_card()` |
| **`build_version_artifacts`** | Orchestrator: validate train/val → write stats + card |
| **`processed_version`** | Config key mirroring folder name |

## Guided code reading

1. `litevla/data/versioning.py` — `VERSION_PATTERN`, `build_version_artifacts`, `render_dataset_card`.
2. `scripts/validate_dataset.py --version --write-artifacts`.
3. `scripts/build_starter_dataset.py --write-artifacts`.
4. `configs/default.example.yaml` — `data.processed_version`.
5. `tests/test_dataset_versioning.py`.

## API contract and data flow

```text
build_starter_dataset()  or  manual JSONL
        │
        ▼
build_version_artifacts(version="v0.1.0", train_jsonl=..., val_jsonl=...)
        │
        ├── validate_dataset(train) ──> validation_report.json
        ├── validate_dataset(val)   ──> validation_report_val.json
        └── render_dataset_card()   ──> DATASET_CARD.md

data/processed/v0.1.0/
├── train.jsonl
├── val.jsonl
├── validation_report.json
└── DATASET_CARD.md
```

| Convention | Rule |
|------------|------|
| Version id | `^v\d+\.\d+\.\d+$` — e.g. `v0.1.0` |
| Stats file | `DatasetValidationReport.to_dict()` as JSON |
| Card | Markdown from `render_dataset_card()` |
| Config mirror | `configs/default.example.yaml` → `data.processed_version` |

### Naive approach vs chosen approach

| Approach | Why it seems attractive | Why we did or did not choose it |
|----------|-------------------------|----------------------------------|
| `latest/` symlink only | Convenient | Ambiguous for configs and papers |
| Date-stamped folders only | Chronological | Hard to reference in YAML |
| Hand-maintained dataset README | Flexible | Drifts from actual counts |
| Generated card + validator JSON | Requires scripts | Always matches on-disk JSONL |

## Implementation breakdown

### Version naming (10105)

**Snippet** (`litevla/data/versioning.py`):

```python
VERSION_PATTERN = re.compile(r"^v\d+\.\d+\.\d+$")

def processed_dir(version: str, *, repo_root=None) -> Path:
    if not is_valid_processed_version(version):
        raise ValueError(...)
    return repo_root / "data" / "processed" / version
```

**Risks and gotchas:** `v0.1` or `0.1.0` are rejected — include the `v` prefix.

---

### Stats and dataset card (10106 / 10107)

`write_dataset_stats()` writes `validation_report.json`. `render_dataset_card()` includes scope, limitations, label/source tables, and validation pass/fail summary.

### CLI integration

```bash
python scripts/build_starter_dataset.py --write-artifacts
python scripts/validate_dataset.py --version v0.1.0 --write-artifacts --skip-image-check
```

## Engineering decisions

```text
ADR: Semver-style folder names (10105)
Status: Accepted
Decision: vMAJOR.MINOR.PATCH directory under data/processed/.
Alternatives Rejected: Date stamps only; unversioned latest/.
```

```text
ADR: Stats from validator not builder (10106)
Status: Accepted
Decision: validation_report.json is output of validate_dataset, not builder counters.
```

## Verification patterns and failure modes

```bash
pytest tests/test_dataset_versioning.py -q
python scripts/validate_dataset.py --version v0.1.0 --write-artifacts --skip-image-check
```

| Symptom | Likely cause | Investigation | Fix |
|---------|--------------|---------------|-----|
| `Invalid processed dataset version` | Missing `v` prefix | Check `--version` flag | Use `v0.1.0` format |
| Card counts don't match JSONL | Stale artifacts | Re-run `--write-artifacts` | Regenerate after data change |
| Training loads wrong version | Config drift | Read `data.processed_version` | Align config with folder |
| validate fails but card exists | Partial write | Check `report.valid` | Fix JSONL before release |

## Engineering principle taught by this task

**Treat dataset releases like API versions.** Folder name, config pointer, QA JSON, and human card should move together as one reproducible unit.

## Active learning checks

1. Why must stats come from the validator, not the builder?
2. What three files should a teammate inspect before trusting `v0.1.0`?
3. Why reject `0.1.0` without the leading `v`?
4. When should you regenerate artifacts vs only rebuild JSONL?

## Open questions

- **Changelog file:** Optional `CHANGELOG.md` per version for human release notes (not MVP).
- **DVC / remote storage:** Large processed sets may move to object storage; paths stay repo-relative for local MVP.

## Related

- [dataset-validation.md](dataset-validation.md) (VLA-45 report source)
- [synthetic-starter-dataset.md](synthetic-starter-dataset.md) (VLA-43 build entry point)
- [`data/README.md`](../../../../data/README.md) (folder layout)
- [`configs/default.example.yaml`](../../../../configs/default.example.yaml)
