# Deployment and quantization experiments. Includes base dependencies.
#
# bitsandbytes requires a CUDA-capable Linux GPU in most setups; install may fail
# on macOS or CPU-only machines. See docs/requirements.md or docs/requirements.html.
-r base.txt

bitsandbytes>=0.43.0
onnxruntime>=1.17.0
