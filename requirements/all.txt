# Full Python stack for local development (base + dev + train + deploy extras).
-r dev.txt
-r train.txt

# Deploy extras are listed separately so a failed optional install does not block setup.
# Install manually when targeting edge/GPU quantization:
#   pip install -r requirements/deploy.txt
